# Marble Popper v0.39 ユーザーガイド

Marble Popperは、進み続けるマーブル列へ砲台から同色のマーブルを撃ち込み、3個以上をそろえて消すブラウザゲームです。100ステージのキャンペーン、無限サバイバル、スコアアタック、カスタムステージのテストを1つのパッケージで遊べます。

このガイドは公開パッケージ版v0.39を対象にしています。

## English quick reference

- Extract the entire ZIP and open `PLAY_MARBLE_POPPER.html` or `index.html` in Chrome/Edge.
- Mouse: move horizontally to position the launcher, left-click to fire vertically, right-click to swap CURRENT/NEXT.
- Touch: tap the playfield to move and fire toward the tap; tap the launcher/ammo area to swap.
- Space pauses/resumes. R restarts the current attempt. F force-clears a non-Survival stage for testing.
- NEW GAME starts from Stage 1; CONTINUE resumes the current stage. High scores are separate for EASY, NORMAL, HARD, and VERY HARD.
- Survival stages unlock by clearing them in the NORMAL campaign.
- Progress is saved in the browser. Use SETTINGS → EXPORT SAVE before changing browser, PC, folder location, or Pages URL.
- Keep `index.html`/`PLAY_MARBLE_POPPER.html` together with the `assets` folder.

## 目次

1. 配布内容と動作環境
2. 起動方法
3. ゲームの目的と基本ルール
4. PC／タッチ操作
5. タイトル画面
6. キャンペーンと難易度
7. WAVEシステム
8. マーブル列、隙間、磁力チェイン
9. 特殊マーブル
10. スコアリング
11. サバイバル
12. ステージテストとカスタムキャンペーン
13. ハイスコア
14. 音声と背景
15. セーブデータ
16. 一時停止、ゲームオーバー、クリア
17. ローカルファイルとプライバシー
18. 制限事項
19. トラブルシューティング
20. バージョンとライセンス

---

## 配布内容と動作環境

主なファイルとフォルダーは次の通りです。

| パス | 内容 |
|---|---|
| `index.html` | GitHub Pages用のゲーム入口。ローカルでも起動可能 |
| `PLAY_MARBLE_POPPER.html` | ローカル利用者向けの同内容のゲーム入口 |
| `assets/` | 背景、BGM、SE用音楽、内蔵100面データ |
| `tools/` | ステージ生成器、評価器、キャンペーンビルダー |
| `USER_GUIDE.md` | このゲームガイド |
| `TOOLS_USER_GUIDE.md` | 制作ツールガイド |
| `LICENSE` | MIT License |
| `MANIFEST.json` | 配布ファイルのサイズとSHA-256 |

### 推奨環境

- Windows 10／11、macOS、一般的なデスクトップLinux
- 現行版のGoogle ChromeまたはMicrosoft Edge
- 1280×720相当以上の画面を推奨
- スマートフォン／タブレットでは横向きを推奨

FirefoxやSafariでも標準Web APIの範囲で動く可能性がありますが、公開版の主対象はChrome／Edgeです。OGG音声の対応やローカルファイルの保存挙動はブラウザによって異なる場合があります。

ネット接続、ユーザー登録、インストーラー、Node.js、Python、ローカルWebサーバーはプレイに不要です。

---

## 起動方法

### ZIPからローカルで遊ぶ

1. ZIPを右クリックして、すべてのファイルを展開します。
2. 展開先の`PLAY_MARBLE_POPPER.html`をダブルクリックします。
3. 開くブラウザを聞かれた場合はChromeまたはEdgeを選びます。
4. タイトル画面のボタンを一度押すと、ブラウザの自動再生制限が解除されBGMを開始できます。

HTMLだけをデスクトップなどへ移動しないでください。ゲームは相対パスで`assets`内のファイルを参照します。フォルダー全体なら別のドライブやUSBメモリへ移動できます。

### GitHub Pagesで遊ぶ

公開URLへアクセスすると`index.html`が起動します。ダウンロードせずに遊べます。ローカル版とPages版はURLが異なるため、ブラウザセーブも別の保存領域になります。

### 画面サイズ

内部ゲーム座標は1280×720です。ブラウザ表示に合わせて縦横比を保ったまま拡大・縮小します。ブラウザのズームを変更してもゲーム内の当たり判定は内部座標で計算されます。

---

## ゲームの目的と基本ルール

レーンの`IN`からマーブルが入り、`OUT`へ進みます。1個でもOUTへ到達するとゲームオーバーです。予定されたすべてのWAVEをしのぎ、レーン上の全マーブルを消すとステージクリアです。

### 色マッチ

撃ったマーブルが列へ入った結果、同じ色が連続して3個以上になると、そのまとまりを消します。

最初から列に存在する3個以上の並びは、存在するだけでは自動消去されません。撃ち込み、または磁力による再接続がマッチ判定のきっかけになります。

### 弾道

PCでは弾は砲台から真上へ進みます。タッチ操作ではタップ地点へ向けて斜めにも撃てます。弾は実際のマーブルへ当たったときだけ列へ入ります。

- 空のレーンは弾を捕まえません。
- 列と列の隙間を通り抜けられます。
- 画面上端や左右へ抜けた弾は失敗になります。
- 障害物へ当たった弾も失敗になります。
- 交差部では上側レーンのマーブルを狙えます。下側レーンのマーブルは上の橋越しには狙えません。

### CURRENTとNEXT

砲台上のマーブルがCURRENT、その下に一部見えているマーブルがNEXTです。発射するとNEXTがCURRENTへ移り、新しいNEXTが抽選されます。

新しいNEXTがCURRENTと同色だった場合は、同色ばかり続く不快感を減らすため、1回だけ追加抽選します。追加抽選も同じ色なら、そのまま採用します。

最終WAVEをすべて投入した後は、レーン上から消滅した色をCURRENT／NEXTへ新しく出しません。すでに飛行中の弾は変更されません。サバイバルはWAVEが無限に続くため、この終盤色フィルターを通常は行いません。

---

## PC／タッチ操作

### PC

| 操作 | 効果 |
|---|---|
| マウス移動 | 砲台を左右へ移動、照準位置を表示 |
| 左クリック | そのX位置から真上へ発射 |
| 右クリック | CURRENTとNEXTを交換 |
| Space | 一時停止／再開 |
| R | 現在のステージを同じ実行シードで最初からやり直す |
| Esc | ステージテスト／サバイバル選択画面からタイトルへ戻る |
| F | サバイバル以外で強制クリア。公開確認用の隠しコマンド |

`F`は100面クリア、次面遷移、クリア演出、セーブ処理を検査するための機能です。通常プレイのハイスコア競争では使用しないでください。セーフティボーナス演出は通常通り実行されます。

### タッチ／ペン

- プレイフィールドをタップすると砲台がそのX位置へ移動し、タップ方向へ撃ちます。
- 砲台／弾表示の領域をタップするとCURRENTとNEXTを交換します。
- 画面下部も発射領域です。
- タップ地点が砲台より下、または近すぎる場合は真上へ撃ちます。

### 一時的に撃てない状態

クリア確定処理、セーフティボーナス中、次WAVE投入直前の安定待ちでは発射を止めます。その間は照準と砲台に禁止表示が出ます。WAVEマーブルが実際に流入している最中は、既存列が止まっていても砲台移動と射撃ができます。

---

## タイトル画面

### 難易度

ニューゲーム／コンティニューへ適用する難易度を選びます。難易度別に進行状況と面別ハイスコアを保存します。サバイバルとステージテストにはこの選択を適用しません。

### ニューゲーム

選択中の難易度をステージ1から始めます。すでに途中まで進んでいる場合は確認が出ます。ニューゲームで進行位置を戻しても、過去のハイスコアは消しません。

### コンティニュー

前回終了した面、またはゲームオーバーになった面から再開します。ステージをクリアすると、コンティニュー位置は次の面へ進みます。ステージ100をクリアしてキャンペーン完了になると、未完了面としてのコンティニューは表示しません。

### サバイバル

クリア条件なしでWAVEを消し続けるモードです。NORMALキャンペーンでクリア済みのステージが選択可能です。詳細は「サバイバル」を参照してください。

### ハイスコア

キャンペーンの難易度別面記録と、サバイバルのステージ別記録を表示します。

### ステージテスト

現在のキャンペーンから任意のステージを選んで遊びます。JSONデータの読込、カスタムキャンペーンの一時適用もできます。進行アンロックは不要です。

### 設定

言語、ミュート、マスター／BGM／SE音量、セーブ書き出し／読み込みを操作します。

---

## キャンペーンと難易度

内蔵キャンペーンは100面です。10面ごとに1ティア進み、形状難易度とゲームプレイ設定が段階的に変化します。同じ面を選んでも、マーブル色列と特殊マーブルなどの実行乱数はプレイごとに変わります。

### 難易度倍率

NORMALはキャンペーンデータをそのまま使います。他の難易度は次を掛け合わせます。

| 難易度 | 弾速 | 列の前進速度 | 色の非連続性 |
|---|---:|---:|---:|
| EASY | ×1.20 | ×0.95 | ×0.95 |
| NORMAL | ×1.00 | ×1.00 | ×1.00 |
| HARD | ×0.95 | ×1.05 | ×1.20 |
| VERY HARD | ×0.95 | ×1.15 | ×1.40 |

「色の非連続性」は、キャンペーンが指定した同色ラン長の相対重みに作用します。高いほど1個や短い並びが相対的に増え、マッチを作りにくくなります。単純に「隣が必ず違う色」へする値ではありません。

ハイスコアとコンティニュー位置は4難易度で独立しています。サバイバルの初期値にはNORMALの元設定を使用します。

---

## WAVEシステム

マーブルは1個ずつ永続的に湧くのではなく、一定数をまとめたWAVEとして入ります。

1. WAVE間のカウントダウンが進みます。
2. 時間になると、飛行弾、消去演出、磁力移動が安定するまで短い準備状態になります。
3. 安定した瞬間に既存列の通常前進を止め、新しいWAVEをINから高速投入します。
4. 既存マーブルがあれば、新しいIN側の列が追いついて連結します。
5. 全WAVEマーブルがレーンへ入った後、最後尾がINから所定距離へ出るまで、物理的につながった列を押します。
6. 通常前進を再開し、次のWAVEタイマーを開始します。

投入中にマーブルを消すことができます。投入メンバーは個別IDで追跡されるため、先頭や途中のマーブルが消えても投入処理を完了できます。列として切れているOUT側の島は、IN側の投入列が実際に接触するまで押されません。

盤面を次WAVE前に空にすると、待機時間を短縮します。その際、短縮前の残り時間を使ってWAVE CLEAR BONUSを計算します。

---

## マーブル列、隙間、磁力チェイン

### 列の進み方

前進力は、IN側にある駆動列の最後尾から伝わります。消去によってOUT側の列が切り離されると、その島は自力では前進しません。後ろから新しい駆動列が追いつくか、色が一致して磁力が働くまでその場に残ります。

### 磁力

マッチで隙間が開き、その両側の端が同色なら、OUT側の列が後ろへ引かれます。接触時に境界をまたいで3個以上になると、磁力チェインとして続けて消えます。

色が違う場合、磁力は働きません。通常前進で物理的に追いついた場合は連結しますが、その接触だけで既存の3個並びを自動消去しません。

### ギャップショット

離れた列の物理的な隙間を1つ以上通過し、そのショットが最終的に色マッチを起こすとギャップショットボーナスです。多くの隙間、狭い隙間を通すほど高得点になります。

---

## 特殊マーブル

特殊マーブルは「色を持つ通常マーブル＋特殊属性」です。そのマーブル自身を色マッチに含めて消したときだけ発動します。BOMBやCOLOR WIPEで直接壊された特殊マーブルは連鎖発動しません。

### プラス効果

| 表示 | 効果 |
|---|---|
| BOMB | 周囲のマーブルを爆破する |
| WIPE | 同じ色のマーブルを盤面から消す |
| BACK | 駆動列を一定時間、IN方向へ戻す |
| SLOW | 列の通常前進を一定時間遅くする |
| SHOT FAST | 発射弾を一定時間速くする |

プラス特殊は磁力チェイン消去への報酬として生成されます。COLOR WIPEは強力なため、候補内でBOMBより低い比率です。

### マイナス効果

| 表示 | 効果 |
|---|---|
| DRIVE FAST | 列の前進を一定時間速くする |
| SHOT SLOW | 発射弾を一定時間遅くする |

同色Aの2個の間へ別色Bを撃つA-B-A型の分断ショットでは、挿入したBが35%の決定的抽選でマイナス特殊になることがあります。

同種効果を重ねた場合、残り時間を単純加算せず、現在時間と新しい所定時間の長い方を使います。BACK中はBACKが前進速度効果より優先します。

---

## スコアリング

このゲームには面クリアだけでなく、技術を評価するスコアアタックがあります。ステージ全体のクリア時間ボーナスはありません。

### 得点項目

| 項目 | 現在の基準 |
|---|---:|
| マーブル消去 | 色マッチ1個につき100 |
| 特殊消去 | BOMB／WIPEによる追加消去1個につき35 |
| 特殊発動 | 発動1回につき100 |
| ショットストリーク | 5連続成功から250、以後1成功ごとに+50増加 |
| 磁力チェイン | 深度2から300、3で700、4で1200、5で1800、6で2500、その先も増加 |
| ギャップショット | 通過隙間数を基礎に500／1250／2250…、狭さで×1.5または×2 |
| WAVE CLEAR BONUS | 次WAVEまでの残り1秒につき500 |
| セーフティボーナス | 最後に消えた最前方位置からOUTまで、マーブル間隔1つにつき10 |

係数は公開候補時点の調整値です。スコア方式が変わる版では、古い版の記録と直接比較できないよう内部のスコアバージョンを分けます。

### 成功ショットとストリーク

発射ごとに識別子を割り当てます。その弾が直接マッチを起こすか、同じ弾から派生した磁力再接続がマッチを起こすと成功です。連鎖が何回起きても、その発射に対する成功回数は1回です。

画面外へ抜ける、障害物へ当たる、磁力接続が消去なしで終わるなど、成功しなかった発射でストリークは0へ戻ります。交換は発射ではないためストリークを壊しません。

### WAVE CLEAR BONUS

次のWAVEが残っている状態で盤面を空にすると、最後のマーブルを消した位置に固定表示を出し、実際の待機中に得点をカウントアップします。待機短縮後の時間ではなく、空になった瞬間の本来の残り時間が得点基準です。

### セーフティボーナス

最終消去直前に存在していたマーブル群のうち、最もOUTに近い位置を記録します。過去に一度だけ進んだ最遠位置は使いません。最終消去が通常マッチ、磁力、BOMB、WIPEのどれでも同じです。

その位置からOUTへ向けてレーン上を`10`表示が高速で進み、最後にOUTで爆発します。演出完了後にリザルトへ移ります。

### リザルト明細

ステージ終了画面では全得点項目、合計、発射数、成功率、最大ストリーク、最大磁力チェイン、ギャップショット回数を表示します。サバイバルでは生存時間も表示します。

---

## サバイバル

好きなアンロックステージで、終わりなく供給されるWAVEに耐えます。OUT到達で終了し、最長生存時間を記録します。同じ時間ならスコアが高い記録を優先します。

### アンロック

NORMALキャンペーンでクリアしたステージだけ選べます。EASY、HARD、VERY HARDのクリアはサバイバルアンロックに使いません。カスタムキャンペーンをメインへ適用した場合は、そのキャンペーンのNORMAL記録を参照します。

### 初期難度

選んだステージのNORMAL GameplayProfileが0秒時点です。キャンペーン難易度選択のEASY／HARD倍率は使いません。

### 圧力上昇間隔

ティア1は15秒ごと、ティア2は16秒ごと、以後1秒ずつ伸び、ティア10は24秒ごとです。画面下部に圧力イベント番号、直近の変更、次の変更までの秒数を表示します。

### 圧力デッキ

色数アップ以外では、現在まだ上限へ達していない項目から1つを選びます。同じ項目だけが偏らないよう、利用可能な項目をシャッフルしたデッキとして一巡させます。

- チェーン速度 ×1.04。初期値の最大2倍まで
- ショット速度 ×0.98。初期値の70%まで
- 色の非連続性 ×1.05。最大2倍まで
- WAVE間隔 -0.2秒。下限3.5秒
- WAVEサイズ +1。初期値+20、かつ80個まで

9個目と18個目の圧力イベントでは、5色未満なら色を1つ増やします。そのとき他の圧力項目を0秒時点へ戻し、新しい色段階でデッキを作り直します。すでに5色なら通常の圧力上昇を行います。

圧力が変わるたびに、緑系、水色系、マゼンタ系の3背景パレットが順に切り替わります。画面中央にも変更内容を一時表示します。

---

## ステージテストとカスタムキャンペーン

ステージテストは進行保存に影響しない自由プレイです。起動時から現在のメインキャンペーン全ステージを一覧で選べます。各PLAYで新しいランダムシードを使うため、レーン形状は同じでも色列や特殊の展開は変わります。

### 読み込めるJSON

- StageSpec v1／v2 1件
- GeneratorのStage Bundle
- EvaluatorのEvaluated Stage Bundle
- CampaignSpec v1＋GameplayProfile v5

StageSpecや形状バンドルだけを読み込んだ場合、ゲームは内蔵のテスト用GameplayProfileを使います。キャンペーンを読み込んだ場合は各レベルに割り当てられたv5プロファイルで遊びます。

### 読み込んだキャンペーンをメインへ適用

v5キャンペーン読込後に「メインキャンペーンとして使用」を押すと、そのセッションのニューゲーム、コンティニュー、サバイバル、ハイスコア対象を差し替えます。

- キャンペーンJSON本体はブラウザへ永続保存しません。
- ページを開き直したら、同じJSONをもう一度読み込みます。
- 記録は内容から作った識別領域へ保存され、同じキャンペーンを再読込すると再表示されます。
- 内蔵キャンペーンに戻すボタンで標準100面へ戻れます。
- 内蔵とカスタムの記録は混ざりません。

制作方法は`TOOLS_USER_GUIDE.md`を参照してください。

---

## ハイスコア

### キャンペーン

ステージをクリアしたときだけ記録します。ゲームオーバーの途中得点は面別ハイスコアへ残しません。従来記録より高い場合に更新します。

難易度選択を変えると、その難易度の面別一覧へ切り替わります。ニューゲームで進行位置をリセットしても記録は残ります。

### サバイバル

OUT到達時の生存時間とスコアをステージ別に記録します。生存時間が長い方を優先し、実質同時間の場合に高スコアを優先します。

---

## 音声と背景

### 音楽

- 通常BGM: ゲーム中に再生。ループ境界前後を約0.5秒フェード
- パニックBGM: 最前方マーブルがOUTまで残り約16%以内でクロスフェードしてループ。約23%より安全側へ戻ると通常BGMへ復帰
- ステージクリア曲: セーフティボーナス完了後、リザルト画面に入ってからループ
- ゲームオーバー曲: 通常／サバイバルのゲームオーバー画面でループ

ブラウザの自動再生制限により、ページを開いただけでは音楽を開始できません。ニューゲーム、PLAY、メニュー操作など最初のユーザー操作と連動して音声を有効化します。

### 音量

- マスター音量: BGMとSEの全体倍率
- BGM音量: 4種類の音楽の倍率
- SE音量: 発射、挿入、消去、WAVE流入、接続、ボーナスなどの倍率
- AUDIO ON／OFF: 全体ミュート

音声設定はブラウザへ自動保存しますが、ポータブルなプレイヤーセーブには含めません。

### 通常BGMの差し替え

ローカル配布では`assets/audio/bgm.ogg`を、同じ名前のOGGファイルへ置き換えられます。元ファイルをバックアップしてください。ループ境界に約0.5秒のフェード処理が入ります。

### 背景

1枚の背景画像にCSSの色調変換を加え、3種類のパレットとして表示します。通常キャンペーンではステージごと、サバイバルでは圧力上昇ごとに変化します。画像自体を差し替える場合は`assets/background.jpg`を同名で置き換えます。

---

## セーブデータ

### 自動保存するもの

- 選択中のキャンペーン難易度
- キャンペーンごと・難易度ごとの進行位置と完了状態
- キャンペーンごと・難易度ごと・面ごとの最高クリアスコア
- キャンペーンごと・面ごとのサバイバル最長時間と同時間時スコア
- 言語、音量、ミュート（音声用の別保存領域）

### 自動保存しないもの

- プレイ途中の盤面
- 飛行中の弾、現在のWAVE、効果残り時間
- 読み込んだカスタムキャンペーンJSON本体
- ステージテストのプレイ結果

### セーブを書き出す

設定 → セーブを書き出すで、`marble-popper-player-save`形式のJSONを保存します。フォルダー移動、別PC、別ブラウザ、ローカル版からPages版への移行前に使用してください。

### セーブを読み込む

設定 → セーブを読み込むでJSONを選び、確認に同意します。読み込みは全置換ではなく統合です。

- より先の進行を残す
- より高いクリアスコアを残す
- より長いサバイバル時間を残す
- 同時間ならより高いサバイバルスコアを残す
- 現在読み込んでいないカスタムキャンペーンの記録も保持する
- 音量とミュートは統合しない

形式、バージョン、数値、危険なキーを検証し、対応しないファイルは読み込みません。

### 保存が消える／別になる条件

- HTMLフォルダーを移動し、ブラウザが別の`file://`領域として扱った
- GitHub Pagesのドメインまたはパスを変更した
- 別のブラウザ、別プロファイルを使用した
- プライベートブラウズを使用した
- サイトデータを削除した
- ブラウザがローカルファイルの`localStorage`を制限した

重要な進行はJSONへ書き出してください。

---

## 一時停止、ゲームオーバー、クリア

右上の`Ⅱ`またはSpaceで一時停止します。ウィンドウが非表示になった、またはフォーカスを失った場合も自動で一時停止します。

一時停止画面では再開、やり直し、ステージ選択、タイトルを選べます。キャンペーン中は自由なステージ選択へ移らないため、該当ボタンの扱いがテスト／サバイバルと異なります。

OUTへ到達するとゲームオーバーです。キャンペーンのコンティニュー位置はその面に残ります。サバイバルでは記録判定後にリザルトを表示します。

最終マーブル消去後は、残っている消去演出、セーフティボーナス、OUT爆発を終えてからステージクリア画面へ入ります。キャンペーンでは次面、同じ面の再プレイ、タイトルへ進めます。100面完了時はキャンペーンクリアとして扱います。

---

## ローカルファイルとプライバシー

アプリは静的HTML、JavaScript、Canvas、Audio、File API、`localStorage`で動きます。ゲームやツールが利用者のJSONを外部サーバーへ送る機能はありません。ファイル選択で読み込んだ内容はブラウザ内で解析します。

GitHub Pagesへ置いた場合、HTML、画像、音声の取得アクセスは通常のWebホスティングとしてGitHub側に記録される可能性があります。ただし、選択したローカルJSONをゲームがGitHubへアップロードするわけではありません。

---

## 制限事項

- プレイ途中の盤面セーブはありません。
- オンラインランキング、ユーザーアカウント、マルチプレイはありません。
- カスタムキャンペーン本体はページ再読込後に自動復元しません。
- スマートフォンでは端末性能、ブラウザUI、タッチ遅延により操作感が変わります。
- キーボードだけですべての照準操作を行う機能はありません。
- スクリーンリーダーだけでゲーム盤面を認識して完遂する設計ではありません。
- OGGのデコード可否はブラウザに依存します。
- StageSpec v1は互換読込用です。新規制作はStageSpec v2／GameplayProfile v5を使用してください。

---

## トラブルシューティング

### 背景や音が出ない

HTMLだけを別の場所へ移していないか確認します。`assets`を含む配布フォルダー全体を展開してください。ZIPの中から直接HTMLを開かず、先に展開してください。

### BGMが最初から鳴らない

ブラウザの自動再生制限です。ニューゲームやPLAYなどを一度押してください。AUDIO、各音量、ブラウザのタブミュート、OS音量、出力デバイスも確認します。

### SEだけ小さい／大きい

設定のSE音量とマスター音量を調整します。BGM音量とは独立しています。

### クリックしても撃てない

- 一時停止、ゲーム終了、セーフティボーナス中ではないか
- 次WAVE直前の「WAVE準備中」ではないか
- 発射直後の短いクールダウン中ではないか
- 照準／砲台に禁止マークが出ていないか

空のレーンを狙った弾は列へ入らず、そのまま画面外へ抜けます。これは仕様です。

### 下側レーンを狙えない

交差部で上のレーンが被さっている場合、下側マーブルは橋越しに狙えません。別の位置、列の移動、斜めに撃てるタッチ操作を利用します。

### コンティニューがない

選択中の難易度でまだニューゲームを始めていないか、100面を完了しています。難易度を切り替えると別の進行状態になります。

### サバイバルでステージが選べない

NORMALキャンペーンで対象面をクリアしてください。別難易度のクリアではアンロックしません。カスタムキャンペーンを適用中なら、そのキャンペーンのNORMAL記録が必要です。

### セーブが見えなくなった

以前と同じブラウザ、プロファイル、HTMLの場所またはPages URLか確認します。書き出したセーブJSONがあれば、設定から統合読み込みします。

### カスタムキャンペーンを再起動後に使えない

仕様です。キャンペーン本体は保存しません。ステージテストで同じv5 JSONを読み込み、「メインキャンペーンとして使用」を再度押します。記録は同じ内容なら復元されます。

### JSONを読み込めない

ゲームが読めるのはStageSpec v1/v2、各バンドル、GameplayProfile v5のCampaignSpecです。Campaign Builder v0.8で書き出したキャンペーンを使用してください。エディターで手修正した場合はJSONのカンマ、引用符、必須フィールドを確認します。

### ゲームが重い

- 他の重いタブやアプリを閉じる
- ブラウザズームを極端に上げない
- ハードウェアアクセラレーションを有効にする
- スマートフォンでは横向きにし、画面録画を止める
- 非常に複雑なカスタムレーンや大量障害物を避ける

### GitHub Pagesで404になる

`index.html`がPages公開ルートにあること、`assets`の階層を変えていないことを確認します。公開直後は反映まで数分待ちます。

---

## バージョンとライセンス

- 製品名: Marble Popper
- ゲームバージョン: v0.39
- 内蔵キャンペーン: 100 levels / GameplayProfile v5
- 配布形式: 静的HTML＋画像／OGG／キャンペーンデータ＋制作ツール
- ライセンス: MIT License
- Copyright (c) 2026 cityedge

ライセンス全文は`LICENSE`を参照してください。
