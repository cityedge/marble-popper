# Marble Popper 制作ツールユーザーガイド

このガイドは、公開パッケージに含まれる3つのブラウザツールを使い、レーン形状の生成からプレイ可能なキャンペーンJSONの完成まで進める方法を説明します。

- Stage Generator v0.19
- Difficulty Evaluator v0.7
- Campaign Builder v0.9

各ツールは自己完結したHTMLです。インストールやWebサーバーは不要で、選んだJSONを外部へ送信しません。

## English workflow summary

1. Open `tools/STAGE_GENERATOR.html`, set a seed and geometry controls, then **Generate Batch** and export the Stage Bundle.
2. Open `tools/DIFFICULTY_EVALUATOR.html`, load the bundle, inspect the heatmap/ranking, then export the Evaluated Bundle.
3. Open `tools/CAMPAIGN_BUILDER.html`, load the evaluated bundle, tune each ten-stage tier, then export a GameplayProfile v5 Campaign JSON.
4. Open the game → STAGE TEST → LOAD DATA, select the campaign, and optionally choose **USE AS MAIN CAMPAIGN**.

The generator and evaluator UI are currently English. The Campaign Builder has Japanese/English switching. Every control is documented below.

## 目次

1. ツールの役割とデータ境界
2. 共通の起動・保存方法
3. Stage Generator
4. Difficulty Evaluator
5. Campaign Builder
6. ゲームでのテスト
7. データ形式
8. 推奨調整手順
9. トラブルシューティング
10. バージョン情報

---

## ツールの役割とデータ境界

制作工程は、形状とゲームプレイ難度を分離しています。

```text
StageSpec v2
  = レーン、交差、障害物、形状メトリクス

GameplayProfile v5
  = 速度、色数、色の並び、WAVE、総マーブル数

CampaignSpec v1
  = 順番、ティア、StageSpec、GameplayProfileの組合せ
```

GeneratorはStageSpecだけを作ります。Evaluatorも形状だけを評価し、色数や速度を評価しません。Campaign Builderで初めてゲームプレイ難度を追加します。

この分離により、同じレーンへ異なる難度設定を割り当てたり、ゲームプレイ値を変えずに新しい形状100件へ差し替えたりできます。

### 標準100面の構造

Generatorは10種類の形状ファミリーを固定順で繰り返します。100件なら各ファミリー10件です。Evaluatorは全体順位とファミリー内順位を付けます。Builderはファミリー内順位1の10件をTier 1、順位2の10件をTier 2として、10ティアを作ります。各ティア内では形状評価の易しい順に並べます。

したがって、Builderへ渡す標準バンドルには次が必要です。

- 件数が10の倍数
- ちょうど10ファミリー
- 各ファミリーが同数
- 各ファミリー内順位が1から連続

---

## 共通の起動・保存方法

### 起動

配布フォルダーの`tools`を開き、目的のHTMLをダブルクリックします。

| ファイル | ツール |
|---|---|
| `STAGE_GENERATOR.html` | Stage Generator v0.19 |
| `DIFFICULTY_EVALUATOR.html` | Difficulty Evaluator v0.7 |
| `CAMPAIGN_BUILDER.html` | Campaign Builder v0.9 |

ChromeまたはEdgeを推奨します。同時に3つのタブで開き、ダウンロードフォルダーを介してJSONを順番に渡すと効率的です。

### 保存

ブラウザツールは入力元ファイルを上書きしません。書き出しボタンを押すと、新しいJSONまたはCSVをブラウザのダウンロード先へ保存します。同名ファイルがあると、ブラウザが連番を付ける場合があります。

制作途中の重要ファイルには、用途と日付が分かる名前を付けて別フォルダーへ保管してください。例:

```text
stage_bundle_100_seed1001.json
evaluated_100_2026-08-25.json
campaign_balance_test_03.json
tier_settings_10_release_candidate.json
```

---

# Stage Generator v0.19

## 役割

1280×720のゲーム座標内に、SPAWNからBASEまでの単一レーンを生成します。自己交差はファミリーに応じて0～2個です。1面ずつのStageSpec v2、または複数面のStage Bundleを出力します。

生成は、ファミリー固有の基礎形状を作り、広い領域へ経路を追加し、簡略化し、丸めて密な点列へ変換する多段処理です。長い経路や厳しい条件では探索時間が増えます。

## 基本操作

### 1面を作る

1. `Seed`を入力します。
2. `Base shape`と長さ関係の設定を選びます。
3. **Generate**を押します。
4. Final表示と下部統計を確認します。
5. 必要ならSimplifyやObstaclesを変更します。
6. **Export StageSpec**で1面JSONを保存します。

### 一括で作る

1. `Batch stages`へ件数を入力します。標準キャンペーンは100です。
2. 上段で長さ、到達率、Simplify、Obstaclesを設定します。
3. **Generate Batch**を押します。
4. 完了を待ちます。無効な候補は自動的に別シードで再試行します。
5. 完了時に自動ダウンロードされます。再度保存する場合は**Export Last Batch**を押します。

バッチ中の`Base shape`選択は使いません。10ファミリーを固定順で循環します。Vertical Flipは各面ごとに独立した50%抽選です。

## 上段パラメータ

### Seed

- 範囲: 1以上の整数
- 初期値: 1001

形状生成の再現キーです。同じGenerator版、同じシード、同じ形状・設定なら同じ結果を再現できます。JavaScript内部では32bit符号なし整数として扱います。

バッチでは開始シードです。候補を1件試すごとにシードを進め、失敗候補も消費します。そのため、生成条件を変えると同じ開始シードでも採用される各面のシードが変わる場合があります。

### Base shape

1面生成の基礎ファミリーを選びます。`Random`はシードから1種類を選びます。

| 表示 | ファミリー | 特徴／交差目標 |
|---|---|---|
| Straight | `straight` | 直線を起点に広げる。交差0 |
| S Curve | `s` | S字。交差0 |
| Horseshoe / U | `horseshoe` | U字／馬蹄形。交差0 |
| Rectangular Wave | `rectwave` | 直角系の往復。交差0 |
| Smooth Serpentine | `serpentine` | 滑らかな蛇行。交差0 |
| Circular Spiral | `spiral` | 円形の渦。交差0 |
| Rectangular Spiral | `rectspiral` | 直角系の渦。交差0 |
| Open Loop | `loop` | 閉じ切らないループ。交差0 |
| One Crossing | `cross1` | 自己交差1 |
| Two Crossings | `cross2` | 自己交差2。AABB／ABAB／ABBA型を含む |

ファミリーは完成形を完全固定するテンプレートではありません。探索によって大きく変形しながら、交差数や基礎的な性格を保ちます。

### Target length

- 範囲: 3000～9000px
- 刻み: 100px
- 初期値: 6000px

生成段階で目標にするSPAWNからBASEまでのレーン中心線長です。大きいほど画面内を長く巡り、同じ速度ならBASE到達までの時間が増えます。小さいほどWAVEの押し込みや速度の影響が強くなります。

これはFinalの長さを厳密固定する値ではありません。探索は許容帯を目標にし、その後のSimplifyで短くなることがあります。長さ調整時はGenerated lengthとFinal lengthの両方を見ます。

### Length tolerance

- 範囲: ±200～±1500px
- 刻み: 100px
- 初期値: ±800px

Target lengthに対して許容する生成帯です。6000±800ならGenerated段階で5200～6800pxが目標です。

小さい値は長さを揃えやすい一方、探索失敗と再試行が増えます。大きい値は生成が安定し、ファミリーの多様性が出ますが、面ごとの長さ差が広がります。

### Minimum screen reach

- 範囲: 45～98%
- 刻み: 1%
- 初期値: 82%

使用可能領域の幅と高さについて、レーンの外接範囲がそれぞれ最低何%へ届くかを指定します。長さとは別の制約です。

短いレーンでも画面の左右・上下を広く使わせられます。高すぎると、特に短いTarget lengthや交差ファミリーで探索が難しくなります。統計の`Generated reach X/Y`が両方とも目標以上なら達成です。

### Simplify strength

- 範囲: 0～200
- 刻み: 1
- 初期値: 72

Generated経路から細かな折れ、冗長点、局所的な蛇行をどの程度取り除くかを選ぶ仕上げ強度です。値が高いほど一般に点数とレーン長が減り、大きく滑らかな形になります。

保護対象:

- ファミリー固有の交差数と交差トポロジー
- レーン同士の最低クリアランス
- 画面端の境界
- SPAWN／BASEと重要骨格
- 最低角度条件

値を変えると、生成探索をやり直さず現在のステージへ簡略化を再適用します。長さを厳密に比較するときは低めで確認し、見た目とデータ量を仕上げる段階で上げます。

### Vertical flip

- `Off`: 上下反転しない
- `On`: 必ず上下反転
- `Random 50%`: シードに基づき50%で反転
- 初期値: Random 50%

使用可能なレーン領域の中で上下反転します。砲台を含むキャンバス全体の中央ではなく、レーン生成領域を基準に変換するため、境界条件を保ちます。

バッチは常にRandom 50%を使用し、選択欄のOff／Onは1面生成だけへ適用します。出力の`source.verticalFlip`と`verticalFlipMode`に記録します。

## ボタン

### Generate

現在のSeed、Base shape、長さ、到達率、Simplify、Vertical flip、Obstaclesで1面を生成します。

### Next Seed

Seedを1増やして生成します。近いシード同士が似る保証はありません。

### Random Seed

ブラウザの暗号学的乱数APIから32bitシードを作り、生成します。

### Export StageSpec

現在のFinal形状をStageSpec v2 JSONとして保存します。失敗状態では保存できません。

ファイル名／`stageId`にはファミリー、シード、目標長、到達率、Simplify、障害物数、Flip有無を含めます。

### Copy Review Shot

ゲーム画面と主要統計を1枚のPNGにまとめ、対応ブラウザではクリップボードへコピーします。クリップボードAPIを使えない`file://`環境ではPNGをダウンロードします。

## バッチ設定

### Batch stages

- 範囲: 1～500
- 初期値: 100

出力する有効ステージ数です。10の倍数なら全ファミリーが同数になります。標準Builderへ渡す場合は10、20、100など10の倍数を使います。

### Generate Batch

`straight → s → horseshoe → rectwave → serpentine → spiral → rectspiral → loop → cross1 → cross2`の順で循環します。各候補を検証し、失敗時は次シードで同じファミリーを再試行します。

現在のTarget length、Length tolerance、Minimum screen reach、Simplify、Obstaclesを全件へ使います。各面のVertical flipは50%抽選です。

### Cancel

現在処理中の1面が終わった後に停止します。生成探索を途中で強制破棄するボタンではないため、重い面では反応まで時間がかかります。途中までの有効面があれば部分バンドルを保持します。

### Export Last Batch

最後に生成したバンドルを再ダウンロードします。次のバッチ開始時に前回バンドルは破棄します。

## プレビュー設定

### Phase

| Phase | 意味 |
|---|---|
| Base | ファミリー固有の最初の骨格 |
| Generated | 長さ・到達率を目指して経路追加した状態 |
| Simplified | Simplifyで骨格点を整理した状態 |
| Final | 曲線を丸め、密な点列へし、障害物を置いた書出し状態 |

Phase切替は観察用です。Exportは常にFinalを使います。

### Sample chain progress

- 範囲: 0～100%
- 初期値: 52%

64個のサンプルマーブル列の先頭を、レーン全長のどこへ置くかを指定します。ゲーム設定や出力データを変えません。交差部の上下描画、曲率、狙いやすさを見るための表示専用です。

### Marbles

- `Show 64-ball sample`: サンプル列を表示
- `Hide`: 非表示

StageSpecへマーブル列は保存しません。

### Obstacles

- 0、2、4
- 初期値: 0

Finalへ配置する障害物数です。円または矩形を、レーン、砲台、重要領域との衝突を避けて配置します。障害物は弾を遮り、ゲーム難度へ大きく影響します。

現在のEvaluatorは障害物による垂直弾道遮蔽を形状評価へ含めます。障害物の位置や形はGeneratorのシードで再現されます。

### Debug

- `Clean`: 通常プレビュー
- `Show coverage / geometry`: 使用可能領域、骨格点、探索グリッド、交差記号を表示

表示専用で、出力は変えません。

## 統計の読み方

### 生成制約

- `Length target`: 目標長と許容差
- `Generated length`: Generated段階の長さ。赤なら許容帯外
- `Reach target`: 幅・高さの最低到達率
- `Generated reach X/Y`: 使用可能幅・高さに対する到達率
- `Generated grid`: 探索格子の使用セル数／全セル数
- `Generated coverage`: レーン近傍が覆う画面セル割合
- `Densify depth`: 経路追加探索の深さ
- `Beam candidates`: 評価した候補数
- `Fallback hits`: 細かい格子など補助探索を採用した回数
- `Whole rescue`: 全体再構成型の救済を採用した回数

### 簡略化

- `Simplify strength`: UI値と選ばれた履歴段階
- `Simplified grid`: 簡略化後の格子使用
- `Simplify tier`: 採用した簡略化レベル
- `Length/Grid/Coverage kept`: Generatedに対する保持率
- `Min skeleton angle`: 骨格の最小角。90度未満は注意表示

### Final形状

- `Phase length`: 選択Phaseのレーン長
- `Phase reach X/Y`: 選択Phaseの到達率
- `Coverage`: 画面被覆率
- `Stretch`: SPAWN-BASE直線距離に対する経路長の倍率
- `Total turn`: 全方向転換量
- `Significant turns`: 大きな方向転換数
- `BBox use`: 外接矩形の画面使用率
- `Meander`: 蛇行の総合参考値
- `Round scale`: Final曲線丸め量

これらは品質判断の補助であり、すべてを最大化する必要はありません。長さ、到達率、狙いやすさ、WAVE圧力、見た目をゲームで確認してください。

## 生成領域と安全距離

キャンバスは1280×720です。58px幅のレーンが画面端からはみ出さないよう、中心線は上下左右で29px以上内側に置きます。下端は砲台にセットされたマーブルとレーン外周が重ならない位置まで使用します。

別のゲーム座標やマーブル径へStageSpecを流用する前提ではありません。互換メタデータはマーブル半径20px、間隔40.5px、交差最大2、`marble-popper-core-v1`です。

---

# Difficulty Evaluator v0.7

## 役割と限界

PCの垂直射撃を前提に、レーンの各位置で「先頭付近のマーブルへ下から弾を通せるか」を仮想評価します。下側交差、手前のマーブル、障害物による遮蔽を調べ、BASE寄りと長く続く難所を重くして0～100程度のスコアへ集約します。下側交差はゲームと同じく、マーブル直径の約2割が上側レーンの下へ入ってから、反対側へ約2割出るまでを遮蔽扱いにします。交差角度が浅いほど実際に覆われる距離が長くなるため、遮蔽区間も長くなります。

評価しないもの:

- チェーン速度
- 発射速度
- 色数と色ラン
- 総マーブル数
- WAVEサイズ／間隔／投入速度
- 特殊マーブル
- 人間の戦術、レーン長に対する実際のクリア余裕

したがって、Evaluatorの順位は形状難度の参考です。Campaign BuilderのGameplayProfileを足した最終難度順位ではありません。特に高い位置のレーンは狙いにくさへ多少反映されますが、弾速による時間差まではシミュレートしません。

## 読み込み

### Load StageSpecs

1つ以上のJSONを選べます。対応形式:

- StageSpec v1／v2 1件
- StageSpec配列
- `stages`配列を持つStage Bundle
- `stageSpecs`配列を持つ旧型バンドル

同じ`stageId`を再読込すると、後から読んだ内容で置き換えます。ファイルをヘッダーへドラッグ＆ドロップすることもできます。

### Clear

読み込んだ全ステージ、選択、フィルターを消します。元のJSONは削除しません。

## 評価モデルパラメータ

### a — evaluation marbles

- 範囲: 1～20の整数
- 初期値: 8

各評価状態で、先頭から何個のマーブルを「確実に存在する評価対象」として並べるかです。各対象の難しさ`h`を合計し、`u(k)=d(k)/a`で0～1へ正規化します。

大きくすると前方グループ全体の相互遮蔽を見る一方、計算量が増え、短い局所難所が平均化されます。小さくすると先頭付近へ敏感になります。

### b — tail decay

- 範囲: 0～0.25
- 刻み: 0.005
- 初期値: 0.015

評価対象よりSPAWN側に続く不確実な後続マーブルの存在強度を、距離`x`に対してどれだけ減らすかです。

```text
p(x) = max(f, 1 - b x)
```

大きいほど遠い後続マーブルを弱く見積もり、小さいほど長い列が続くと仮定します。0では存在する後続すべてを強度1として扱います。

### f — tail floor

- 範囲: 0～1
- 刻み: 0.05
- 初期値: 0.10

後続マーブル存在強度`p(x)`の下限です。遠い後続も完全な0にせず、最低限の遮蔽可能性を残します。

0なら十分遠い後続を無視できます。1なら距離減衰が事実上なく、存在する後続すべてを確実として扱います。

複数の不確実後続が同じ対象を遮る場合、独立確率を合成せず最大の`p(x)`を使います。これは「同じ連続列が少なくともそこまで続く」という入れ子事象だからです。

### c — BASE exponent

- 範囲: 0～8
- 刻み: 0.25
- 初期値: 2

レーン進行率に対するBASE寄り重みの指数です。

```text
w(k) = progress(k)^c
```

0なら全位置を等しく扱います。大きいほどBASE近くの狙いにくさを重視し、SPAWN側難所の影響を小さくします。

### λ — persistence

- 範囲: 0～0.995未満
- UI刻み: 0.01
- 初期値: 0.80

局所難度を指数移動平均へ蓄積する記憶率です。

```text
S(k) = λ S(k-1) + (1-λ) u(k)
```

0なら持続記憶なしで現在状態だけです。1へ近づけるほど、長く続く難所がゆっくり蓄積し、短い1点ピークの影響を抑えます。画面には半減期をマーブルステップ数で表示します。0.80の半減期は約3.1ステップです。

### s — sustained mix

- 範囲: 0～1
- 刻み: 0.05
- 初期値: 0.40

最終スコアで、BASE重み付き平均と持続難度面積を混ぜる比率です。

```text
A_sus = average(S(k)^2)
score = 100 × ((1-s) × D_out + s × A_sus)
```

0ならBASE重み付き局所難度だけ、1なら持続難度面積だけです。二乗するため、赤／橙の高圧区間が長く続く形状を強く評価します。

### Recalculate All

現在の6パラメータで全ステージを再評価し、全体順位とファミリー内順位を付け直します。入力欄でEnterを押しても再計算できます。

## プレビュー

### ヒートマップ

- `Local d(k)`: 各位置の直接的な狙いにくさ
- `Sustained pressure`: λで蓄積した持続圧力

緑が易しく、赤が難しい位置です。交差は実際の上下関係に合わせて再描画します。

### Stateスライダー

仮想列先頭の位置をマーブル間隔単位で移動します。選択状態について次を表示します。

- `head`: レーン全長に対する先頭進行率
- `d`: a個の難しさ合計
- `S`: 持続圧力
- 各Targetの位置、難度、遮蔽理由

### show hypothetical marbles

確実な評価対象と、不確実な後続マーブルをプレビューへ表示します。後続は`p(x)`に応じて薄くなります。

### show target status

Target表を表示します。代表的な理由:

- `clear`: 垂直弾道が通る
- `eval #n`: 別の評価マーブルが遮る
- `tail x=n`: 不確実な後続が部分遮蔽
- `lower crossing`: 下側交差で、ゲームと同じ約2割基準の遮蔽区間にあるため狙えない
- `obstacle`: 障害物が弾道を遮る

## ランキング表

### フィルター

- Family: 全体または1ファミリー
- Flip: All／Normal／Flipped／Unknown

フィルターや列見出しソートは表示だけを変えます。書き出す正式順位は常に難易度昇順です。

### 列

- `Rank`: 全体順位。1が最も易しい
- `Fam#`: 同じファミリー内の順位。1が最も易しい
- `Stage`: stageId
- `Family`: ファミリー名
- `Flip`: 上下反転状態
- `Score`: 最終難易度スコア
- `BASE mean`: BASE重み付き平均×100
- `SustArea`: 持続圧力二乗の平均×100
- `SustMax`: 持続圧力の最大×100
- `Final25`: 最後の25%区間の局所難度平均×100
- `Max local`: 局所難度最大×100
- `Length`: レーン長px
- `Batch`: Generatorのバッチ内番号

同点時は`source.batchIndex`、その後`stageId`で順序を確定します。

## 書き出し

### Export Evaluated Bundle

正式順位順の全StageSpecと評価メタデータを、`marble-popper-evaluated-stage-bundle` schemaVersion 2として保存します。Builderへ渡す標準出力です。

評価パラメータ、モデル名、全体順位、ファミリー内順位、形状メトリクスを含みます。元StageSpec自体は変更せず内包します。

### Export Ranking CSV

ランキングと全指標をUTF-8 BOM付きCSVで保存します。Excelなどで比較、グラフ化、外れ値確認をする用途です。CSVはBuilder入力ではありません。

### Export Selected StageSpec

選択中ステージの元StageSpecだけをJSONで再保存します。評価情報は付けません。

---

# Campaign Builder v0.9

## 役割

評価済みStageSpecを10ステージ単位のティアへ並べ、各ティアへGameplayProfile v5を設定し、ゲームが読めるCampaignSpec v1を出力します。

個別ステージごとのGameplayProfile編集は行いません。Tier 1の設定はステージ1～10すべて、Tier 2は11～20すべてへ共有します。

## 言語

右上の`日本語`／`English`で表示を切り替えます。現在の作業データや値は変わりません。

## ワークフロー1: データを読み込む

ドロップ領域をクリックするか、JSONをドラッグ＆ドロップします。

### 評価済みバンドル

新規キャンペーンを作ります。次を検証します。

- `marble-popper-evaluated-stage-bundle` schemaVersion 2
- 全StageSpecがv2
- stageIdが重複しない
- 件数が10の倍数
- 10ファミリー
- 各ファミリーの件数がティア数と同じ
- familyRankが1～ティア数で連続
- StageSpecにゲームプレイ設定が混入していない

読込後、公開既定のティア設定をティア数に合わせて適用します。10ティアなら同梱プリセットの値を正確に再現します。ティア数が異なる場合は同じ曲線上を補間します。

### 既存キャンペーン

CampaignSpec v1を読み込むと、面順、StageSpec、ティア設定、Campaign ID、名前を復元して再編集できます。

- GameplayProfile v5: 全設定を復元
- v4: 旧挙動維持のためLong-run tailを0として移行
- v3: 色連続性はv5既定へ置換し、総数、色数、速度、WAVEなど他の設定を復元

旧形式互換は固定10ステージティアのキャンペーンだけが対象です。

## ワークフロー2: データ概要

- ステージ数
- ファミリー数
- ティア数
- 全ステージの障害物数合計

意図した100／10／10になっているか確認します。

## ワークフロー3: ティア設定の保存・読み込み

### 設定を書き出す

現在の全ティアのGameplayProfileだけを`marble-popper-tier-profile-set` schemaVersion 1として保存します。StageSpecや面順を含まないため、新しく生成した別の100面へ同じ難度曲線を適用できます。

保存内容:

- ティア数
- 1ティア10面という規則
- GameplayProfile v5配列
- タイトル、作成日時、元Campaign ID

### 設定を読み込む

現在のステージと順番を保持し、ティア設定だけ置き換えます。対応入力:

- 専用Tier Profile Set
- GameplayProfile v3／v4／v5を持つ既存CampaignSpec

現在のティア数と入力のティア数が一致する必要があります。確認後、同じ番号のティア同士を置き換えます。

同梱の`tools/presets/marble-popper-main-v5_10_tier_settings_v5.json`は公開版10ティアの基準です。

## ワークフロー4: ティア曲線を自動補間

1. `パラメーター`を1つ選びます。
2. Tier 1値と最終Tier値を入力します。
3. `全ティアへ補間`を押します。

選んだ値だけを等間隔の線形補間で全ティアへ設定します。他の値は変更しません。補間後に右側インスペクターで個別調整できます。

補間できる全項目と入力範囲:

| 項目 | 範囲 | 刻み |
|---|---:|---:|
| チェーン速度 | 1～250 | 1 |
| 射出速度倍率 | 0.4～2.0 | 0.01 |
| 色数 | 2～5 | 1 |
| 総マーブル数 | 1～1000 | 1 |
| WAVEマーブル数 | 3～80 | 1 |
| WAVE間隔 | 1～15秒 | 0.25 |
| 盤面クリア時のWAVE間隔 | 0.25～5秒 | 0.25 |
| 投入後の余裕 | 0～5個分 | 0.25 |
| WAVE投入速度 | 200～1200px/s | 10 |
| 1／2／3／4+個ラン重み | 0～100 | 1 |
| 長ランテール | 0～1 | 0.05 |
| 単独色の連続上限 | 1～4 | 1 |
| 単独色上限の超過率 | 0～1 | 0.01 |

## ティア設定一覧

中央の最初のタブです。各行をクリックすると右側へそのティアを表示します。

- ティア番号
- 対応レベル範囲
- 平均レーン長
- チェーン速度
- 総マーブル数
- 色数
- 射出速度倍率
- WAVEサイズ
- 押込率
- WAVE間隔

`押込率`は、1WAVE分のマーブル長と投入後余裕がレーン長に占める割合の参考値です。短いレーン、大きいWAVE、大きい余裕ほど上がり、投入1回の圧迫感が強くなります。

## キャンペーン順

中央の2つ目のタブです。最終的なレベル番号、ティア、ファミリー、形状評価値、GameplayProfile ID、StageSpec IDを確認します。

並びは固定です。

1. ファミリー内順位が同じ10件を1ティアへ集める
2. Tier 1はfamilyRank 1、Tier 2は2…
3. 各ティア内を形状Difficultyの昇順に並べる

任意位置でティア境界を作る機能や、面を手動ドラッグして入れ替える機能はありません。

## ティア別プロファイル: 全パラメータ

右側のスライダーと数値欄は同期します。数値欄ではスライダー表示範囲より広い検証範囲を使える項目があります。

### チェーン速度 / baseSpeed

- スライダー: 30～180px/s
- 数値欄／形式上: 1～250px/s
- 大きいほど難しい

通常時に駆動列がBASEへ進む基準速度です。実際には難易度倍率、BACK、SLOW、DRIVE FASTなどが掛かります。レーン長との比が重要で、同じ108px/sでも3000pxと7000pxのレーンでは猶予が異なります。

### 射出速度倍率 / projectileSpeedScale

- 範囲: 0.4～2.0
- 刻み: 0.01
- 小さいほど難しい

弾の開始速度、加速度、最高速度すべてへ同じ倍率を掛けます。遅い弾は重量感が増しますが、高いレーンや曲がった奥の列へ到着するまでに標的が動くため、狙いが難しくなります。

NORMAL以外のキャンペーン難易度倍率や、SHOT FAST／SHOT SLOWもさらに掛かります。

### マーブルの色数 / colorCount

- 2～5色
- 多いほど難しい

赤、青、黄、緑、紫の先頭から使用する色数です。色数が増えると同色弾と標的の一致率が下がり、孤立色も処理しにくくなります。サバイバルでは最大5色まで段階的に増えます。

### 総マーブル数 / totalBalls

- スライダー: 20～600
- 数値欄／形式上: 1～1000

キャンペーン1面で予定する全マーブル数です。最後のWAVEは、残数がWAVEサイズ未満なら残りだけ投入します。多いほどプレイ時間、失敗機会、得点可能量が増えます。

サバイバルは無限供給のため、この上限を終了条件に使いません。

### WAVEマーブル数 / waveSize

- 範囲: 3～80個
- 大きいほど一般に難しい

1回の通常WAVEへ予定する個数です。全数をレーンへ入れたうえで投入後余裕まで押すため、大きいほど列全体を一度に前へ進めやすくなります。短いレーンでは特に強く作用します。

### WAVE間隔 / waveInterval

- 範囲: 1～15秒
- 刻み: 0.25秒
- 小さいほど一般に難しい

WAVE投入が完了して通常移動へ戻ってから、次WAVE準備へ入るまでの時間です。投入そのものにかかる時間は含みません。

短すぎると、1WAVEを処理する物理時間より先に次の圧力が来ます。標準サバイバルの動的下限は3.5秒ですが、Builderではキャンペーン実験のため1秒まで入力できます。

### 盤面クリア時のWAVE間隔 / emptyBoardInterval

- 範囲: 0.25～5秒
- 刻み: 0.25秒

次WAVE待ち中に盤面を空にしたとき、残り待機を最大何秒へ短縮するかです。通常のWAVE間隔が5秒、現在残り3秒、値1.25なら残りを1.25秒へ縮めます。

WAVE CLEAR BONUSは短縮前の残り時間から計算するため、この値を小さくしても獲得可能点そのものは減りません。主に空盤面で待たされる時間を調整するQOL値です。

### 投入後の余裕 / entryPaddingMarbles

- 範囲: 0～5
- 刻み: 0.25
- 単位: マーブル間隔

WAVE最後尾がレーンへ入った後、最後尾をSPAWNからさらに何個分進めるかです。2なら、最後尾中心がSPAWNから約2×40.5pxの位置へ来るまで、物理的につながった投入列を押します。

列が途中で切れている場合、接触していないBASE側の島まで一括移動させません。投入列が実際に追いついた後は連結先へ圧力が伝わります。

### WAVE投入速度 / deploymentSpeed

- 範囲: 200～1200px/s
- 刻み: 10px/s

新WAVEがSPAWNから流入し、投入後余裕まで押し込む速度です。通常のチェーン速度とは別です。大きいほど投入演出が短く、急激な圧力になります。

投入中は通常前進を止めますが、プレイヤーは発射できます。速度が高すぎると移動中の標的を狙いにくくなります。

### 物理ラン長の相対重み / runLengthWeights

1個、2個、3個、4個以上の同色ランを選ぶ相対重みです。0～100で、合計100にする必要はありません。

例:

```text
1個=20, 2個=30, 3個=40, 4個以上=10
```

基礎抽選比率は20:30:40:10です。ラン境界では必ず前のランと別色にします。つまり「ラン長」と「色」の抽選を分けています。

1個を増やすと孤立色が増えて難しくなりやすく、3～4個を増やすと既存の消しやすい塊が増えます。ただし4個が極端に多く、色数が少ないと、3～4個ずつ順番に色が並ぶ単調な列になりやすくなります。

ゲーム難易度のHARD／VERY HARDにある色の非連続性倍率は、短いラン側を相対的に強める形でこの分布へ作用します。

### 長ランテール / longRunTailScale

- 範囲: 0～1
- 刻み: 0.05

4個以上に割り当てた重みから、5～7個の珍しい長ランをどれだけ作るかです。

1.00のとき、4個の重みを基準に次を追加します。

- 5個 = 4個重み ×20%
- 6個 = 4個重み ×10%
- 7個 = 4個重み ×5%

0.50なら上記追加分を半分、0なら4個で打ち切りです。4個の重みそのものを5～7へ置き換えるのではなく、展開後の候補へ追加するため、画面上の実比率は全重みで再正規化されます。

「簡単なティアでも時々5～7個が出る」自然な裾を作り、4個上限に張り付く規則性を崩します。

### 画面上の予測比率

現在の相対重みとテールを、ラン数ではなくマーブル個数ベースへ換算して表示します。長いランは1回出るだけで多くのマーブルを占めるため、抽選確率と画面占有率は一致しません。

`平均ラン`は1つの同色ランに含まれる予測平均マーブル数です。

### 単独色の連続上限 / singletonStreakLimit

- 範囲: 1～4

1個ランがこの回数続いた後、次のランを原則2個以上にします。例えば2なら、1個、1個と続いた後の次ラン抽選から1個候補を除きます。

難しいティアで1個重みを高くしても、異なる孤立色が3つ4つと不自然に連続するのを抑えます。

### 単独色上限の超過率 / singletonOverflowChance

- 範囲: 0～1
- 刻み: 0.01

単独色上限へ達した後でも、例外としてもう1回1個ランを許す確率です。

- 0: 上限を厳守
- 0.10: 10%でさらに1個ランを許可
- 1: 上限による抑制なし

完全な禁止による機械的パターンを避けたい場合に小さい値を使います。

## 公開既定の10ティア

公開既定は同梱プリセットから読み込まれます。主要値は次の通りです。WAVE間隔5秒、盤面クリア時1.25秒、投入後余裕2個、投入速度640px/s、長ランテール1、単独上限2、超過率0.10は全ティア共通です。

| Tier | 総数 | 色 | チェーン | 射出倍率 | 1/2/3/4+重み | WAVE |
|---:|---:|---:|---:|---:|---|---:|
| 1 | 280 | 3 | 108 | 1.13 | 15/20/30/25 | 16 |
| 2 | 290 | 3 | 110 | 1.11 | 20/25/25/20 | 16 |
| 3 | 300 | 3 | 112 | 1.09 | 25/30/20/15 | 17 |
| 4 | 310 | 4 | 113 | 1.07 | 20/20/30/20 | 17 |
| 5 | 320 | 4 | 115 | 1.05 | 25/25/25/15 | 18 |
| 6 | 330 | 4 | 117 | 1.03 | 30/30/20/10 | 18 |
| 7 | 340 | 5 | 119 | 1.01 | 25/20/30/15 | 19 |
| 8 | 350 | 5 | 120 | 0.99 | 25/30/25/10 | 19 |
| 9 | 360 | 5 | 122 | 0.97 | 30/30/15/5 | 20 |
| 10 | 370 | 5 | 124 | 0.95 | 30/20/10/5 | 20 |

これは開始点であり、すべての新規形状セットに対して同じ体感難度を保証するものではありません。レーン長が短ければWAVE押し込みが強くなり、上側レーンが多ければ遅い弾の影響が強くなります。

## Campaign ID

セーブ領域を識別する基本IDです。英小文字、数字、`-`、`_`中心の安定した値を推奨します。

内容が異なる公開キャンペーンへ同じIDを使うと、内蔵運用やツール上で混同する可能性があります。大きく別物ならIDを変えます。ゲームが外部キャンペーンをセッション適用するときは、内容フィンガープリントも使って記録を分離します。

## キャンペーン名

ゲームのタイトル画面やステージテストで人が読む名前です。日本語も使用できます。ファイル名の安全化にはCampaign IDを使います。

## キャンペーンJSONを書き出す

全プロファイルとレベル構造を検証し、自己完結したCampaignSpec v1を保存します。各レベルに次を含めます。

- レベル番号、ティア、ティア内順位
- 参照GameplayProfile ID
- 形状評価値、全体／ファミリー内順位
- StageSpec v2本体

Builderの作業ファイルを別途保存する概念はありません。書き出したCampaign JSON自体を次回Builderへ読み込めるため、それが途中保存・再編集ファイルになります。

---

## ゲームでのテスト

1. ゲームを開く。
2. **ステージテスト**を選ぶ。
3. **データを読み込む**でBuilderのCampaign JSONを選ぶ。
4. ステージ一覧から序盤、中盤、色数変更直後、Tier 10を選んで遊ぶ。
5. キャンペーン全体として試す場合は**メインキャンペーンとして使用**を押す。

### 最低限の試遊点

- Tier 1: 同色3～4個が基本だが、5～7個の珍しいテールが出るか
- 高Tier: 1個が増えるが、孤立色だけが3～4回続きすぎないか
- 色数変更面: 直前ティアから難度が急増しすぎないか
- 短いレーン: 1WAVEで押される割合が過大でないか
- 高いレーン: 弾速が遅すぎて狙いが成立しない区間がないか
- 交差: 上下関係と弾の通過が視覚通りか
- 障害物: 不可能な射線や砲台封鎖がないか
- 最終WAVE: 存在しない色の弾が残り続けないか
- WAVE投入中: 消去しても流入が止まらず、離れたBASE側島が不自然に押されないか

100面すべてを通常クリアせず遷移確認する場合は、ゲーム中に`F`キーで強制クリアできます。サバイバルでは無効です。

---

## データ形式早見表

| データ | format | schemaVersion | 主な作成元 | 主な読込先 |
|---|---|---:|---|---|
| StageSpec | `marble-popper-stage` | 2 | Generator | Evaluator、Game |
| Stage Bundle | `marble-popper-stage-bundle` | 2 | Generator | Evaluator、Game |
| Evaluated Bundle | `marble-popper-evaluated-stage-bundle` | 2 | Evaluator | Builder、Game |
| GameplayProfile | `marble-popper-gameplay-profile` | 5 | Builder内部 | Campaign、Game |
| Tier Settings | `marble-popper-tier-profile-set` | 1 | Builder | Builder |
| Campaign | `marble-popper-campaign` | 1 | Builder | Builder、Game |
| Player Save | `marble-popper-player-save` | 1 | Game | Game |

### 互換性方針

- 新規制作はStageSpec v2、GameplayProfile v5を使用
- EvaluatorとGameはStageSpec v1も互換読込
- Builderは固定10面ティアのv3／v4／v5 Campaignを再編集可能
- Gameのメイン適用はv5 Campaignのみ
- Player Saveと制作JSONは用途が異なり、相互に読めない

JSONをテキストエディターで手修正できますが、文字コードはUTF-8、数値は有限値、JSONとして正しい引用符とカンマが必要です。まずツールのUIで編集することを推奨します。

---

## 推奨調整手順

### 形状セットを作り直す場合

1. Generatorで10面の小バッチを作る。
2. レーン長、到達率、生成時間、見た目を確認する。
3. Gameのステージテストで数面遊ぶ。
4. 問題がなければ100面を作る。
5. Evaluatorで外れ値とヒートマップを確認する。
6. Builderで既存Tier Settingsを読み込む。
7. 短い／長いレーンのWAVE押込率を見て調整する。
8. Tier 1、4、7、10を重点試遊する。

### 色ランを調整する場合

1. まず1～4+の重みを決める。
2. Long-run tailを加え、画面上の予測比率を見る。
3. 平均ランが増えすぎたら4+重みを下げる。
4. 難しいティアで1個を増やす。
5. Singleton limit／overflowで孤立色の連発だけを抑える。
6. NORMALとVERY HARDの両方で確認する。

### WAVEを調整する場合

WAVEサイズ単独ではなく、次の組合せで見ます。

```text
押し込みの強さ ≈ (WAVEサイズ + 投入後余裕) × マーブル間隔 ÷ レーン長
```

さらに、チェーン速度、WAVE間隔、総マーブル数、弾速、色ランが実際の処理余力を決めます。後半だけ急に厳しい場合、WAVEサイズを下げる以外に、レーン長を揃える、間隔を延ばす、弾速を上げる選択肢があります。

### 評価器を再調整する場合

評価済みデータには使用パラメータを記録します。異なるパラメータで作ったスコアを同じ列として混ぜず、全100面を同一設定で再計算してください。

まず既定値で遊び、明らかな逆転例を複数集めてから変更します。1面だけに合わせると全体順位を悪化させることがあります。

---

## トラブルシューティング

### Generatorが非常に遅い

- Target lengthが長い
- Length toleranceが狭い
- Minimum screen reachが高い
- 複雑な交差／渦ファミリー
- 使用可能領域が広く、探索自由度が高い

まず10面で時間を測ります。許容差を広げる、到達率を少し下げる、目標長を短くする順で試します。Cancelは現在の1面処理後に効きます。

### Generatorが目標をMISSする

統計でLengthとReachのどちらかを確認します。両方の制約を同時に満たせない組合せがあります。Simplify後のFinal lengthが短いだけなら、Target lengthを上げるかSimplifyを下げます。

### BuilderがEvaluated Bundleを拒否する

- 10の倍数でない
- 10ファミリーでない
- 各ファミリー数が違う
- familyRankが欠けている
- StageSpec v1が混ざっている
- StageSpec内に`gameplay`がある

標準GeneratorのバッチをEvaluator v0.7で評価し直すのが最も確実です。

### Tier Settingsを読み込めない

現在のティア数と設定ファイルのティア数を一致させます。100面は10ティア、20面は2ティアです。専用設定またはCampaign JSONを選びます。Player Saveは読めません。

### キャンペーン再読込で色設定が変わった

v3 Campaignの旧色連続性はv5ラン長モデルと互換でないため、v5既定へ置き換えます。v4は長ランテールを0として旧挙動を保ちます。完全復元が必要ならv5 CampaignまたはTier Settingsを保存してください。

### Evaluatorの順位と体感が合わない

Evaluatorは形状とPC垂直弾道だけを評価します。弾速、WAVE、色数、レーン長に対する時間余裕は総合評価しません。同一ファミリー内の形状順を作る補助と考え、最終判断はGameで行います。

### JSONをゲームへ入れてもメインが変わらない

読込だけではステージテスト候補になります。CampaignSpec v1＋全GameplayProfile v5の場合に表示される「メインキャンペーンとして使用」を押してください。ページ再読込後は再度読み込みが必要です。

### ダウンロードボタンが反応しない

ブラウザの複数ダウンロード許可、ダウンロードブロック、保存先の空き容量を確認します。同じページで大量のファイルを連続保存すると確認が出る場合があります。

### Copy Review Shotがコピーでなく保存になる

ローカル`file://`ではClipboard APIが制限されることがあります。PNGダウンロードへ自動フォールバックする正常動作です。

---

## バージョン情報

- Stage Generator Lab: v0.19
- Difficulty Evaluator: v0.7
- Campaign Builder: v0.9
- StageSpec: v2
- Evaluated Stage Bundle: v2
- GameplayProfile: v5
- CampaignSpec: v1
- ゲーム互換版: Marble Popper v0.54
- ライセンス: MIT License

ツールの計算式や出力形式を改造して公開する場合は、バージョンやモデル名を変更し、生成データの`source`／`evaluation`へ由来を残してください。
